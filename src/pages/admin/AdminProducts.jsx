import { useState } from 'react'
const initial=[{name:'Mesa de bilhar metálica',sku:'MB-001',price:95000},{name:'Taco profissional',sku:'TC-010',price:2500},{name:'Bolas completas',sku:'BL-004',price:7500}]
export default function AdminProducts(){const [items,setItems]=useState(initial); const [name,setName]=useState(''); function add(){if(!name)return;setItems([...items,{name,sku:'NOVO',price:0}]);setName('')} return <><div className="admin-head"><div><span className="eyebrow">Catálogo</span><h1>Produtos</h1></div></div><div className="panel inline-form"><input placeholder="Nome do novo produto" value={name} onChange={e=>setName(e.target.value)}/><button className="btn" onClick={add}>Adicionar</button></div><div className="panel"><table><thead><tr><th>Produto</th><th>SKU</th><th>Preço</th><th>Ação</th></tr></thead><tbody>{items.map((p,i)=><tr key={i}><td>{p.name}</td><td>{p.sku}</td><td>{p.price.toLocaleString('pt-PT')} MT</td><td><button className="link-btn" onClick={()=>setItems(items.filter((_,x)=>x!==i))}>Remover</button></td></tr>)}</tbody></table></div></>}
